#include "lists.h"
#include <string.h>
#include <stdlib.h>
/**
 * main - generate a ket depending on a username
 * @argc: number of arguments passed
 * @argv: arguments passed to main
 *
 * Return: 0 on success, 1 on error
 */
int main(int argc, char *argv[])
